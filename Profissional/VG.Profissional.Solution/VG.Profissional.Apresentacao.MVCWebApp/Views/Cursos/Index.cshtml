@model IEnumerable<VG.Profissional.Aplicacao.ViewModels.CursoViewModel>

@{
    ViewBag.Title = "Lista de Cursos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Criar Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CurriculoId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Instituicao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Curriculo.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Instituicao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.CursoId }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.CursoId }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.CursoId })
        </td>
    </tr>
}

</table>
